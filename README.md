# Data-Analysis-Task-2
    TASK 2: CREDIT_RISK_PREDICTION
            STEP 1: Import Libraries
            STEP 2: Virtual Environment
            STEP 3: Select the kernel
            STEP 4: Load dataset
            STEP 5: SHOW the Dataset Head
            STEP 6: Handle Missing data
            STEP 7: Visualizing the key features like (Loan ammount, Education, Income Distribution)
            STEP 8: Target Variables (Y = Default)
            STEP 9: Train - Test - Split (Test size = 0.2, random state = 42)
            STEP 10: Train model (Logistic Regression)
            STEP 11: Fit model (X_train, Y_train)
            STEP 12: Evaluate Model (Prediction, Accuracy, Confusion Matrix)
            STEP 13: Push into Github_repository
            STEP 14: END.....
            